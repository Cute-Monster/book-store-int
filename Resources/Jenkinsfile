pipeline {
    agent {
        label 'Slave'
    }

    environment {
        CR_GITLAB_TOKEN = credentials('cruntov_gitlab_st')
    }
    stages {
        stage('Git Checkout'){
            steps{
                echo("Hello There")
            }
        }
    }
}